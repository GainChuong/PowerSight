'use client';

import React from 'react';

export default function FullscreenEnforcer() {
  // Violation reporting for fullscreen has been disabled as requested.
  // Fullscreen is now enforced automatically by the extension background script.
  return null;
}
